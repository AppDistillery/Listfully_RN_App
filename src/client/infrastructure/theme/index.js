import { colors } from './colors';
import { fontSizes, fontWeights, fonts } from './fonts';
import { lineHeights, sizes, space } from './sizesAndSpacing';

export const theme = {
  colors,
  fontSizes,
  fontWeights,
  fonts,
  lineHeights,
  sizes,
  space
};
